<div class="AD_text">
  <div class="AD_scrollDiv">
    <%ads= Advert.ip_advert(request.remote_ip)%>
    <ul>
      <%= request.remote_ip %>
      <% ads.each do |ad| %>
        <li><%= ad.content %></li>
      <%end unless ads.blank?%>
    </ul>
  </div>
</div>
